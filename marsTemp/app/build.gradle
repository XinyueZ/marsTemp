def buildNumber = 'bld' + (System.env.BUILD_NUMBER ?: 'SNAPSHOT')
apply plugin: 'com.android.application'

android {
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    compileSdkVersion Integer.parseInt(project.ANDROID_COMPILE_SDK_VERSION)
    buildToolsVersion project.ANDROID_BUILD_TOOLS_VERSION
    defaultConfig {
        applicationId project.APP_APPLICATIONID
        minSdkVersion Integer.parseInt(project.ANDROID_BUILD_MIN_SDK_VERSION)
        targetSdkVersion Integer.parseInt(project.ANDROID_BUILD_TARGET_SDK_VERSION)
        versionCode Integer.parseInt(project.APP_VERSIONCODE)
        versionName project.APP_VERSIONNAME
    }
    buildTypes {
        debug {
            versionNameSuffix buildNumber
        }
        release {

        }
    }

    //This will be required to use the Volley Library on SDK 23 and higher
    useLibrary 'org.apache.http.legacy'

    dataBinding {
        enabled = true
    }

    lintOptions {
        htmlReport false
        abortOnError false
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile project(':ChoppingBootstrap')


    compile("com.android.support:support-compat:$rootProject.ext.supportLib4")
    compile("com.android.support:support-core-utils:$rootProject.ext.supportLib4")
    compile("com.android.support:support-core-ui:$rootProject.ext.supportLib4")
    compile("com.android.support:support-media-compat:$rootProject.ext.supportLib4")
    compile("com.android.support:appcompat-v7:$rootProject.ext.supportLib7") {
        exclude module: 'support-v4'
    }
    compile("com.android.support:design:$rootProject.ext.supportDesign")
    compile("com.android.support:cardview-v7:$rootProject.ext.cardView")
    compile("com.android.support:recyclerview-v7:$rootProject.ext.recycler")
    compile("com.google.android.gms:play-services:$rootProject.ext.play")


    compile("com.github.shell-software:fab:$rootProject.ext.fabLib")
    compile("com.squareup.retrofit:retrofit:$rootProject.ext.retrofit")
    compile("com.squareup.okhttp:okhttp-urlconnection:$rootProject.ext.okhttpUrlConnection")
    compile("com.squareup.okhttp:okhttp:$rootProject.ext.okhttp")
}

// Rename final apks
//android.applicationVariants.all {  variant ->
//    variant.outputs.each  { output ->
//        output.outputFile = new File(
//                output.outputFile.parent, output.outputFile.name.replace("_Android", "")
//                .replace(".apk", "-${android.defaultConfig.versionName}${buildNumber}.apk"))
//    }
//}

